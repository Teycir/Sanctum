# DuressVault Environment Variables (Example)
# Copy this file to .env.local and configure for your environment

# ============================================================================
# ARCHITECTURE: Browser-only, zero backend, RAM-only mandatory
# ============================================================================
# DuressVault has NO backend server, NO database, NO API routes.
# All crypto operations happen in the browser.
# IPFS storage is handled by Helia (browser IPFS node).
# ============================================================================

# Application Settings
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================================
# DEVELOPMENT ONLY: Optional debugging flags
# ============================================================================

# Enable verbose logging in browser console
NEXT_PUBLIC_DEBUG=false

# Disable RAM-only mode for testing (DANGEROUS - for dev only)
# NEXT_PUBLIC_DISABLE_RAM_ONLY=false

# Mock IPFS for testing (use in-memory storage)
# NEXT_PUBLIC_MOCK_IPFS=false
